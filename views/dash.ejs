<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Add your CSS file if needed -->
</head>
<body>
    <div class="container">
        <h1>Welcome <%= user.first_name %> <%= user.last_name %></h1>

        <% if (user) { %>
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>CIN:</strong> <%= user.cin %></p>
            <p><strong>Sex:</strong> <%= user.sexe %></p>
            <p><strong>Phone:</strong> <%= user.tel %></p>
            <p><strong>Family Status:</strong> <%= user.family_statut %></p>
            <% if (user.doti) { %>
                <p><strong>DOTI:</strong> <%= user.doti %></p>
            <% } %>
            <p><strong>Role:</strong> <%= user.role === 1 ? 'Admin' : 'User' %></p>

            <a href="auth/logout">Logout</a>
        <% } else { %>
            <p>You are not logged in. <a href="/login">Login here</a></p>
        <% } %>
    </div>
</body>
</html>
